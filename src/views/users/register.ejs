<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <script src="/js/users/registerValidation.js"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet" />
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <link rel="stylesheet" href="/css/register.css">

  </head>

<body>
  <section class="contenedor-formulario">

    
        <form action="/users/register" method="post">
            <h1 class="titulo-formulario">DH-Records</h1>
            <div class= "contenedor-casilla">
                <input class="casilla-individal" type="text" name="nombreUsuario" id="nombreUsuario" placeholder="Username"  value="<%= oldData ? oldData.nombreUsuario : '' %>">
                <i class='bx bxs-user'></i>
                <div class="error__message--name" ></div>
                <% if( locals.errores && errores.nombreUsuario) { %>
                    <div class="error">
                        <%= errores.nombreUsuario.msg %>
                    </div>
                <% } %>
            </div>  

            <div class= "contenedor-casilla">
                <input class="casilla-individal" type="email" name="email" id="email" placeholder="Email address" value="<%= oldData ? oldData.email : '' %>">                
                <i class='bx bxs-envelope'></i>
                <div class="error__message--email" ></div>
                <% if( locals.errores && errores.email) { %>
                    <div class="error">
                        <%= errores.email.msg %>
                    </div>
                <% } %>
            </div> 

            <div class= "contenedor-casilla">
                <input class="casilla-individal" type="password" name="contraseña" id="contraseña" placeholder="Password" value="<%= oldData ? oldData.contraseña : '' %>">
                <i class='bx bxs-lock-alt'></i>
                <div class="error__message--password" ></div>
                <% if( locals.errores && errores.contraseña) { %>
                    <div class="error">
                        <%= errores.contraseña.msg %>
                    </div>
                <% } %>
            </div>

            <div class="terminosCondiciones-checkbox">
                <label>
                    <input type="checkbox" name="terminosCondiciones">
                    I accept the terms and conditions
                </label>
            </div>

            <button type="submit" class="boton-registro">Sing Up</button>

            <div class= "link-registro">
                <p>Have an account? <a href="/users/login"> Log in</a> </p>
            </div> 
             
             
        </form>
    </section>
</body>

      