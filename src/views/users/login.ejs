<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <script src="/js/users/loginValidation.js"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet" />
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <link rel="stylesheet" href="/css/login.css">
</head>

<body>
  <section class="contenedor-formulario">
        <form action="/users/login" method="post">
            <h1 class="titulo-formulario">DH-Records</h1>
            <div class= "contenedor-casilla">
                <input class="casilla-individal" type="email" name="email" id="email" placeholder="Email address" value="<%= oldData ? oldData.email : '' %>" >
                <i class='bx bxs-user'></i> 
                <div class="error__message" ></div>
                <% if( locals.errores && errores.email) { %>
                    <div class="error">
                        <%= errores.email.msg %>
                    </div>
                <% } %>
            </div>  

            <div class= "contenedor-casilla">
                <input class="casilla-individal" type="password" name="contraseña" id="contraseña" placeholder="Password" value="<%= oldData ? oldData.contraseña : '' %>">
                <i class='bx bxs-lock-alt'></i>
                <div class="error__message" ></div>
                <% if( locals.errores && errores.contraseña) { %>
                    <div class="error">
                        <%= errores.contraseña.msg %>
                    </div>
                <% } %>
            </div> 

            <div class="contenedor__opciones">
                <div class="rememberMe-checkbox">
                    <label>
                        <input type="checkbox" name="rememberUser">
                        Remember me
                    </label>
                </div>

                <div class= "recuperar-contraseña">
                    <a href="#" class="olvidaste-contraseña">¿Forgot your password?</a>
                </div>    
            </div>

            <button type="submit" class="boton-login">Log in</button>

            <div class= "link-registro">
                <p>Don't have an account? <a href="/users/register"> Sing up</a> </p>
            </div> 
             
             
        </form>
    </section>
</body>

      